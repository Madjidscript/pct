import React from 'react';

const AdReclamationClient = (props) => {
    return (
        
        < div>
          reclamaation client
        </ div>
    );
};

export default AdReclamationClient;